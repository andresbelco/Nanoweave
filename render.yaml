services:
  - type: web
    name: nanoweave-tea-dashboard
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn nanoweave_tea_dash:server
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
